[phases.setup]
nixPkgs = ['...', 'php82', 'nodejs', 'npm']

[phases.build]
cmds = [
  'composer install --no-dev --optimize-autoloader --no-interaction',
  'npm ci --no-audit --prefer-offline',
  'npm run build'
]

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'